# Heavy dependencies mocked when building optimum.intel docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
openvino
nodeps:optimum
real:transformers<5
real:numpy
real:packaging
real:huggingface_hub
real:diffusers
