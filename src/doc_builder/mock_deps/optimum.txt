# Heavy dependencies mocked when building optimum docs (see mock_imports.py).
# Shared by optimum-onnx, whose docs also build under the library name `optimum`
# (only --repo_name differs): onnxruntime/onnx belong to that build.
# Packages that are actually installed are never mocked.
torch
onnxruntime
onnx
