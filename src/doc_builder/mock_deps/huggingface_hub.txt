# Heavy dependencies mocked when building huggingface_hub docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
real:filelock
real:fsspec
real:packaging
real:pyyaml
real:requests
real:tqdm
real:typing_extensions
