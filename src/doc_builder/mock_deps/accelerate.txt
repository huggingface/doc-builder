# Heavy dependencies mocked when building accelerate docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
deepspeed
torchvision
real:numpy
real:packaging
real:psutil
real:pyyaml
real:safetensors
real:huggingface_hub
