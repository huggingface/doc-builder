# Heavy dependencies mocked when building peft docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
real:numpy
real:packaging
real:psutil
real:pyyaml
real:transformers
real:tqdm
real:safetensors
real:huggingface_hub
nodeps:accelerate
