# Heavy dependencies mocked when building timm docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
torchvision
real:pyyaml
real:huggingface_hub
real:safetensors
real:numpy
