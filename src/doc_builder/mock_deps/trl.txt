# Heavy dependencies mocked when building trl docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
torch
real:datasets
real:transformers
real:rich
nodeps:accelerate
real:jinja2
real:psutil
nodeps:peft
