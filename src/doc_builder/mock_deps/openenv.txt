# Heavy dependencies mocked when building openenv docs (see mock_imports.py).
# Packages that are actually installed are never mocked.
tomli_w
fastapi
fastmcp
mcp
openai
gradio
websockets
real:requests
real:pydantic
real:aiohttp
real:typer
